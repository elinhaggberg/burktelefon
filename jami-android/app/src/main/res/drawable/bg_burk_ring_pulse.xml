<?xml version="1.0" encoding="utf-8"?>
<!-- Stroke-only ring, scaled/faded from code (BurkAnim.ringPulse) to reproduce the
     "bk-ring" outgoing/incoming pulse animation from the design reference. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="3dp"
        android:color="@color/burk_blue" />
</shape>
